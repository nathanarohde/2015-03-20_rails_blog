<%= comment.user.name %>
<br>
<%= comment.text %>
  <% if (current_user) %>
    <% if (comment.user_id == current_user.id) %>
      <%= link_to 'edit', edit_post_comment_path(@post, comment), id: 'edit_comment', remote: true %>
        <%= link_to 'delete', post_comment_path(@post, comment), :data => {:method => 'delete'} %>
    <% end %>
  <% end %>
<br>
