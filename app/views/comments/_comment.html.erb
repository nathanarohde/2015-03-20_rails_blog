<%= comment.user.name %>
<br>
<%= comment.text %>
  <% if (current_user) %>
    <% if (comment.user_id == current_user.id) %>
      <%= link_to 'Edit', edit_post_comment_path(@post, comment), id: 'edit_comment', remote: true %>
      <%= link_to 'Delete', post_comment_path(@post, comment), class: 'delete', :data => {:method => 'delete'} %>
    <% end %>
  <% end %>
<br>
