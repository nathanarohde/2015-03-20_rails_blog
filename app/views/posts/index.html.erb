<h4>Blog Thingie</h4>

<ul>
  <% @posts.each do |post| %>
  <li><%= link_to post.title, post_path(post) %></li>
  <% end %>
</ul>

<p><%= link_to "Create a New Post", new_post_path, id: 'new_post', remote: true %></p>
